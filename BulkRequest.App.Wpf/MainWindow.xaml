<Window x:Class="BulkRequest.App.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BulkRequest.App.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="40*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <GridSplitter Grid.Row="0" Grid.Column="1" Width="4" HorizontalAlignment="Left" VerticalAlignment="Stretch" Background="#FF007EFF"/>
        <GridSplitter Grid.Row="1" Grid.ColumnSpan="2" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Top" Background="#FF007EFF"/>
        <local:QueryTextEditor x:Name="_queryTextEditor" Grid.Row="0" Grid.Column="1" Margin="4,0,0,0"/>
        <GroupBox Grid.Row="1" Grid.ColumnSpan="2" Margin="0,4,0,0">
            <GroupBox.Header>
                <Label Content="_Журнал" Target="{Binding ElementName=_logTextBox}"/>
            </GroupBox.Header>
            <TextBox x:Name="_logTextBox" Background="Black" BorderBrush="{x:Null}" Foreground="White"/>
        </GroupBox>
    </Grid>
</Window>
